<?xml version="1.0" encoding="ISO-8859-1" ?>

<!--

                    This file was generated by JaCaMo 0.8-SNAPSHOT
                    http://jacamo.sf.net

                    novembro 15, 2018 - 20:19:36
-->

<project name   ="house_building"
         basedir=".."
         default="run">

    <property name="jacamo.project.file" value="house.jcm"/>
    <property name="debug" value=""/> <!-- use "-debug" to run in debug mode -->
    <property name="build.dir" value="${basedir}/bin/classes" />

    <property name="jasonJar" value="/home/maiquel/git/jacamo/build/libs/jason-2.4-SNAPSHOT.jar"/>

    <path id="project.classpath">
        <pathelement location="${basedir}"/>
        <pathelement location="${build.dir}"/>
        <fileset dir="/home/maiquel/git/jacamo/build/libs" > <include name="*.jar" /> </fileset>

        <pathelement location="lib"/>

    </path>

    <!-- tasks the user can override in his/her c-build.xml script -->
    <target name="user-init">
    </target>
    <target name="user-end">
    </target>

    <target name="init">
        <mkdir dir="${build.dir}" />
        <mkdir dir="${basedir}/src/env" />
        <mkdir dir="${basedir}/src/agt" />
        <mkdir dir="${basedir}/log" />
        <antcall target="user-init" />
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${basedir}/." destdir="${build.dir}" debug="true" optimize="true" includeantruntime="false" >
            <classpath refid="project.classpath"/>
        </javac>
        <javac srcdir="${basedir}/src/env" destdir="${build.dir}" debug="true" optimize="true" includeantruntime="false" >
            <classpath refid="project.classpath"/>
        </javac>
        <javac srcdir="${basedir}/src/agt" destdir="${build.dir}" debug="true" optimize="true" includeantruntime="false" >
            <classpath refid="project.classpath"/>
        </javac>
        
    </target>

    <target name="run" depends="compile" >
        <echo message="Running project ${ant.project.name}" />
        <java classname="jacamo.infra.JaCaMoLauncher" failonerror="true" fork="yes" dir="${basedir}" >
            <classpath refid="project.classpath"/>
            <arg line="${jacamo.project.file} ${debug} "/>
            <!-- jvmarg line="-Xmx500M -Xss8M"/ -->
        </java>
        <antcall target="user-end" />
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${basedir}/lib" />
        <delete file="${ant.project.name}.jar" />
        <copy file="${jasonJar}" tofile="house.jar" />
        <copy file="${jacamo.project.file}" tofile="default.jcm" />
        <jar update="yes" jarfile="jacamo-house.jar" >
            <fileset dir="${basedir}">
                <include name="**/*.asl" />
                <include name="**/*.jcm" />
                <include name="**/*.xml" />
            </fileset>
            <fileset dir="${basedir}">
                <include name="logging.properties" />
            </fileset>
            <fileset dir="${build.dir}">
                <include name="**/*.class" />
            </fileset>
            <zipgroupfileset dir="${basedir}/lib" includes="**/*.jar" />
            <zipgroupfileset dir="${jasonJar}/../../libs" includes="*.jar" />
            <manifest>
                  <attribute name="Main-Class" value="jacamo.infra.JaCaMoLauncher"/>
            </manifest>
        </jar>
        <delete file="default.jcm" />
        <echo>-----------------------</echo>
        <echo/>
        <echo>To run your MAS, just type "java -jar jacamo-house.jar"</echo>
    </target>

    <target name="clean" >
        <delete failonerror="no" includeEmptyDirs="true" verbose="true">
            <fileset dir="${basedir}" includes="**/*.class"/>
        </delete>
    </target>



</project>
